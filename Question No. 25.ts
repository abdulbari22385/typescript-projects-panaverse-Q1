let alien_color: string = 'green';

// Passes the if test
if (alien_color === 'green') {
  console.log("The player just earned 5 points!");
}

// Fails the if test
if (alien_color !== 'green') {
  // no output
}
